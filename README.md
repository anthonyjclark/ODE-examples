# ODE-examples

```
# Add submodule
hub submodule add review/logger-cpp

# Make changes and commit then push like normal repo

# Update project with newly committed submodule
hub add path/to/submodule
hub commit -m "Updated submodule"
```
